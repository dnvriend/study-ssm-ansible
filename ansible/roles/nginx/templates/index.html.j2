<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ ansible_hostname }} - Web Server</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .container {
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            padding: 48px;
            max-width: 600px;
            width: 100%;
        }

        h1 {
            color: #333;
            font-size: 32px;
            margin-bottom: 8px;
            font-weight: 700;
        }

        .role-badge {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 32px;
        }

        .info-grid {
            display: grid;
            gap: 16px;
        }

        .info-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px;
            background: #f7f9fc;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }

        .info-label {
            font-weight: 600;
            color: #555;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .info-value {
            color: #333;
            font-size: 16px;
            font-weight: 500;
        }

        .footer {
            margin-top: 32px;
            text-align: center;
            color: #999;
            font-size: 14px;
            padding-top: 24px;
            border-top: 1px solid #eee;
        }

        .ansible-logo {
            color: #667eea;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{ ansible_hostname }}</h1>
        <div class="role-badge">Role: Web Server</div>

        <div class="info-grid">
            <div class="info-item">
                <span class="info-label">Hostname</span>
                <span class="info-value">{{ ansible_hostname }}</span>
            </div>

            <div class="info-item">
                <span class="info-label">Environment</span>
                <span class="info-value">{{ environment | default('N/A') }}</span>
            </div>

            <div class="info-item">
                <span class="info-label">Role</span>
                <span class="info-value">Web Server</span>
            </div>

            <div class="info-item">
                <span class="info-label">Deployed via</span>
                <span class="info-value">SSM + Ansible</span>
            </div>

            <div class="info-item">
                <span class="info-label">Deployment Time</span>
                <span class="info-value">{{ ansible_date_time.iso8601 }}</span>
            </div>

            <div class="info-item">
                <span class="info-label">OS Family</span>
                <span class="info-value">{{ ansible_os_family }}</span>
            </div>

            <div class="info-item">
                <span class="info-label">Architecture</span>
                <span class="info-value">{{ ansible_architecture }}</span>
            </div>
        </div>

        <div class="footer">
            Managed with <span class="ansible-logo">Ansible</span> via AWS Systems Manager
        </div>
    </div>
</body>
</html>
